# mysql-compose

Quick setup for exploring MySQL databases
Do *not* use in production

## Usage
Run:
```
docker-compose up -d
```

Open:
```
localhost:3333
```

Server: `mysql`
Username: `root`
Password: `root`
